@model ActiviteViewModel
   

    @{int x = 4;

        if (ViewBag.show != null)
        {
            foreach (var item in ViewBag.show)
            {
                var dynamic = "#myModal" + @x;


        <button type="button" data-toggle="modal" data-target=@dynamic class="btn btn-default"> @item.Titre</button>
                x = x + 1;
            }
        }
       
    }
  

